/* 
let ms = 1e-3,
    mcs = 1e-6,
    one = 1e0,
    google = 1e100

alert(ms + '\n' + mcs + '\n' + one + '\n' + google);
*/

let num = 174;
alert(num.toString(2));

let thirtySix = 35;
alert(thirtySix.toString(36))

alert(72..toString());